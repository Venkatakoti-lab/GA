name: Github Acrions demo
on: 
  # push:
  #   branches:
  #     - main
  workflow_dispatch:
jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
      - name: Show System Info
        run: uname -a 
      - name: Install Dependencies
        run: echo "Installing Dependencies"
      - name: Build Project
        run: echo "Build Complete"