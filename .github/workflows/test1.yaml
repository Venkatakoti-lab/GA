name: Testing Workflow
on: 
  # push:
  workflow_dispatch:
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Display 
        run: echo "Hello Kiyansh"
      - name: Display The Date
        run: date